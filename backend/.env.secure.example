# ⚠️ SECURITY NOTICE ⚠️
# This is an example file. DO NOT commit actual secrets to version control!
# Copy this to .env and fill in with your actual values
# Make sure .env is in your .gitignore

# ==============================================================================
# SERVER CONFIGURATION
# ==============================================================================
NODE_ENV=production
PORT=3000

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dayflow_hrms
DB_USER=dayflow_user

# 🔒 CRITICAL: Use a strong password (min 16 chars, mixed case, numbers, symbols)
DB_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD_MIN_16_CHARS

# Database pool settings
DB_MAX_CONNECTIONS=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=10000

# ==============================================================================
# JWT CONFIGURATION
# ==============================================================================
# 🔒 CRITICAL: Generate strong random secrets (minimum 32 characters)
# Use this command to generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Access token secret (MUST be at least 32 characters)
JWT_ACCESS_SECRET=GENERATE_A_RANDOM_32_CHAR_SECRET_HERE_DO_NOT_USE_THIS_DEFAULT

# Refresh token secret (MUST be different from access secret and at least 32 characters)
JWT_REFRESH_SECRET=GENERATE_ANOTHER_DIFFERENT_32_CHAR_SECRET_HERE_CHANGE_THIS

# Token expiry times
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Legacy JWT secret (if using jwt.utils.ts)
JWT_SECRET=SAME_AS_ACCESS_SECRET_OR_GENERATE_NEW_32_CHAR_SECRET
JWT_EXPIRES_IN=7d

# ==============================================================================
# EMAIL CONFIGURATION
# ==============================================================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com

# 🔒 For Gmail, use App-Specific Password: https://support.google.com/accounts/answer/185833
EMAIL_PASSWORD=your-app-specific-password-here

EMAIL_FROM=noreply@dayflow-hrms.com

# ==============================================================================
# CORS CONFIGURATION
# ==============================================================================
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000,http://localhost:5173,https://yourdomain.com

# ==============================================================================
# RATE LIMITING
# ==============================================================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# ==============================================================================
# SECURITY HEADERS
# ==============================================================================
# Enable security headers in production
SECURITY_HEADERS_ENABLED=true

# ==============================================================================
# LOGGING
# ==============================================================================
LOG_LEVEL=info
LOG_FILE=logs/app.log

# ==============================================================================
# SESSION (if using sessions)
# ==============================================================================
# 🔒 Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=GENERATE_RANDOM_32_CHAR_SECRET_FOR_SESSIONS

# ==============================================================================
# ADDITIONAL SECURITY
# ==============================================================================
# Maximum file upload size (in bytes)
MAX_FILE_SIZE=5242880

# Allowed file types for uploads (comma-separated)
ALLOWED_FILE_TYPES=image/jpeg,image/png,application/pdf

# ==============================================================================
# SECURITY CHECKLIST
# ==============================================================================
# ✅ All secrets are at least 32 characters
# ✅ All secrets are unique (no duplicates)
# ✅ Database password is strong
# ✅ Email uses app-specific password
# ✅ CORS origins are specific (not wildcard *)
# ✅ This file is in .gitignore
# ✅ Production values are different from development
# ✅ Secrets are stored securely (vault/secrets manager)

# ==============================================================================
# GENERATE SECRETS COMMAND
# ==============================================================================
# Run this in your terminal to generate secure random secrets:
# 
# node -e "
#   const crypto = require('crypto');
#   console.log('JWT_ACCESS_SECRET=' + crypto.randomBytes(32).toString('hex'));
#   console.log('JWT_REFRESH_SECRET=' + crypto.randomBytes(32).toString('hex'));
#   console.log('SESSION_SECRET=' + crypto.randomBytes(32).toString('hex'));
# "
